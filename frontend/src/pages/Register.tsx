import { RegisterForm } from '@/features/auth/components/RegisterForm';

export default function Register() {
    return (
        <div className="flex-1 w-full flex items-center justify-center bg-slate-50 p-4">
            <RegisterForm />
        </div>
    );
}